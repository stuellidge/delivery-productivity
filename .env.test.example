TZ=UTC
PORT=3334
HOST=localhost
LOG_LEVEL=error
APP_KEY=
NODE_ENV=test
SESSION_DRIVER=memory
AUTH_METHOD=database

# Test Database (separate from dev database)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=app
DB_PASSWORD=app
DB_DATABASE=app_test

# Test secrets (any non-empty string works for local testing)
HMAC_KEY=
GITHUB_WEBHOOK_SECRET=
